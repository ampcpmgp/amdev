fs = require("fs")
fse = require("fs-extra")
dir = "template"
fse.removeSync("browser")
fse.removeSync("app")
fse.removeSync("web")
fs.renameSync("#{dir}/browser", "browser")
fs.renameSync("#{dir}/app", "app")
fs.renameSync("#{dir}/web", "web")
fs.renameSync("#{dir}/.gitignore", ".gitignore")
fs.renameSync("#{dir}/package.json", "package.json")
fse.removeSync(dir)
console.log "fin"
